# semantic-pdf-search — Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Tested on Python 3.10 and 3.11 (macOS Apple Silicon, Ubuntu 22.04 x86_64).
# Python 3.13 is NOT supported. Use Python 3.10 or 3.11.
#
# Version note: llama-index-embeddings-huggingface 0.3.x requires
# llama-index-core<0.12.0. All integrations below are pinned to the
# mutually compatible 0.11.x / 0.3.x generation.

# ── LlamaIndex core ──────────────────────────────────────────────────────────
llama-index-core>=0.11.0,<0.12.0

# ── Integrations ─────────────────────────────────────────────────────────────
llama-index-embeddings-huggingface>=0.3.0,<0.4.0
llama-index-vector-stores-qdrant>=0.4.0,<0.5.0
llama-index-llms-ollama>=0.3.0,<0.4.0
llama-index-readers-file>=0.2.0,<0.3.0

# ── Vector database ──────────────────────────────────────────────────────────
qdrant-client>=1.9.0,<2.0.0

# ── Embedding model backend ──────────────────────────────────────────────────
sentence-transformers>=2.7.0
torch>=2.1.0

# ── PDF parsing ──────────────────────────────────────────────────────────────
pypdf>=4.0.0

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.66.0
