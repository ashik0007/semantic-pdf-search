# semantic-pdf-search — Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Tested on Python 3.11, macOS Apple Silicon (M-series) and Ubuntu 22.04 x86_64.
# Python 3.13 is NOT supported. Use Python 3.10 or 3.11.
#
# Version note: All llama-index packages must be from the same generation.
# The versions below are pinned to the 0.14.x series — the proven working set.

# ── LlamaIndex core ──────────────────────────────────────────────────────────
llama-index-core>=0.14.0,<0.15.0

# ── Integrations ─────────────────────────────────────────────────────────────
llama-index-embeddings-huggingface>=0.7.0,<0.8.0
llama-index-vector-stores-qdrant>=0.10.0,<0.11.0
llama-index-llms-ollama>=0.10.0,<0.11.0
llama-index-readers-file>=0.5.0,<0.6.0

# ── Vector database ──────────────────────────────────────────────────────────
qdrant-client>=1.9.0,<2.0.0

# ── Embedding model backend ──────────────────────────────────────────────────
sentence-transformers>=2.7.0
torch>=2.1.0

# ── PDF parsing ──────────────────────────────────────────────────────────────
pypdf>=4.0.0

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.66.0
